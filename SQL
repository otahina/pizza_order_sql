CREATE TABLE orders (
  row_id INT AUTO_INCREMENT PRIMARY KEY,
  order_id VARCHAR(255),
  created_at DATETIME,
  item_id INT,
  quantity INT,
  cust_id INT,
  delivery ENUM('shipped', 'pickup'),
  add_id INT
);
